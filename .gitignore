node_modules/
dist/
*.tsbuildinfo

# Local DuckDB store + caches + user data (regenerated by `npm run seed`)
data/
packages/ingestion/.cache/
**/.cache/

# Static site build outputs (regenerated by `npm run build:site` / CI)
site/data.json
site/dist/

# Env / editor
.env
.env.local
.DS_Store
*.log
